# Intake note — <one-line description>

**Received:** YYYY-MM-DD
**Source:** <email / screenshot / paste / dropped file>
**File:** `inbox/<original-filename>`

## Why is this in `inbox/` and not already routed?

- It is ambiguous (no project has claimed it yet).
- It is a sensitive fragment (credentials, PII, legal) awaiting your
  decision on where it belongs.
- The agent is not sure whether it is a new project or evidence for an
  existing one.

## What is in the file (one-paragraph summary)

<one short paragraph the agent writes from inspecting the file>

## Routing options

- **Option A.** File under `materials/<existing-project>/01-inputs/`.
  Use when: <reason>.
- **Option B.** Create a new project file at
  `memory/projects/<slug>.md` and file under
  `materials/<slug>/01-inputs/`.
  Use when: <reason>.
- **Option C.** Delete or move to `trash/`.
  Use when: <reason>.

## Operator decision

- [ ] Pick option A / B / C.
- [ ] Once decided, the agent moves the file out of `inbox/`, and this
  note either migrates to `inbox/pending-user/` (with the date stamped
  up-to-date) or is deleted once the file is routed.
